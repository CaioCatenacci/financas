{
  "name": "financas",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10"
  }
}
